<div class='mt-4'>
    <h3>Cập nhật Khóa Học</h3>
    <form method='POST' action='/courses/{{course._id}}?_method=PUT'>
        {{! action chọn chỗ nó xược đến thui xong viết ở controller để xử lí cái này, kiểu viết riêng để đỡ bị lẫn }}
        <div class='form-group mt-2'>
            <label for='name'>Tên Khóa Học</label>
            <input
                type='text'
                class='form-control'
                value='{{course.name}}'
                id='name'
                name='name'
            />
        </div>
        <div class='form-group mt-2'>
            <label for='desciption'>Mô tả</label>
            <input
                type='text'
                class='form-control'
                id='desciption'
                name='desciption'
                value='{{course.desciption}}'
            />
        </div>
        <div class='form-group mt-2'>
            <label for='videoId'>Video ID</label>
            <input
                type='text'
                class='form-control'
                id='videoId'
                name='videoId'
                value='{{course.videoId}}'
            />
        </div>
        <div class='form-group mt-2'>
            <label for='level'>Trình độ khóa học</label>
            <input
                type='text'
                class='form-control'
                id='level'
                value='{{course.level}}'
                name='level'
            />
        </div>

        <button type='submit' class='btn btn-primary mt-4'>Lưu lại</button>
    </form>
</div>